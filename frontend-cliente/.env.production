# El portal vive en otro origen (puntos.puntadiamantes.com), así que apunta
# al backend con la URL COMPLETA (no relativa). El backend sirve la API en /api.
VITE_API_URL=https://puntadiamantes.com/api
